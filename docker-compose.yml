services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: rsy
      POSTGRES_PASSWORD: rsy
      POSTGRES_DB: rsy_platform
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  postgres_data:
  redis_data:

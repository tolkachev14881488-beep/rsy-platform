{
  "version": 2,
  "buildCommand": "cd ../.. && npm run db:generate && npm run build --workspace=apps/admin",
  "installCommand": "cd ../.. && npm install",
  "framework": "nextjs",
  "outputDirectory": ".next",
  "crons": [
    {
      "path": "/api/cron/automate",
      "schedule": "0 */6 * * *"
    }
  ]
}
